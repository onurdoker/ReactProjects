const Project5ScoreBoard = ({
  userScore,
  pcScore,
}) => {
  return (
      <div>
        
        <h3>Score Board</h3>
        <p>User Score : {userScore}</p>
        <p>PC Score : {pcScore}</p>
      </div>
  );
};

export default Project5ScoreBoard;

<div>

</div>;